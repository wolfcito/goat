# Solana configuration
SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
SOLANA_WALLET_SEED=your_wallet_private_key_in_base58

# Smolagents model configuration
# Choose ONE of the following model options:

# Option 1: OpenAI API (used in the example by default)
OPENAI_API_KEY=your_openai_api_key

# Option 2: HuggingFace API
# Comment out the OPENAI_API_KEY above and uncomment:
#HF_MODEL_NAME=microsoft/phi-3-mini-4k-instruct
#HF_API_TOKEN=your_huggingface_token_if_needed_for_model_access

# Option 3: Local models - requires code changes in example.py
# Use HfLocalModel instead of OpenAIServerModel

# Option 4: VLLM deployment - requires code changes in example.py
# Use VLLMModel instead of OpenAIServerModel